Implementation in Python: import codecs plaintext = "HELLO" ciphertext = codecs.encode(plaintext, 'rot_13') print(ciphertext) # Outputs: "URYYB" Implementation in JavaScript: var plaintext = "HELLO"; var ciphertext = plaintext.replace(/[A-Za-z]/g, function(c) { return String.fromCharCode((c <= "Z" ? 90 : 122) >= (c = c.charCodeAt(0) + 13) ? c : c - 26); }); console.log(ciphertext); // Outputs: "URYYB"