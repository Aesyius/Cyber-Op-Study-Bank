The OSI layers interact with each other through a process called encapsulation, where each layer adds its header or trailer to the data received from the upper layer before passing it to the lower layer. OSI LayerProtocols/Services/Devices ApplicationHTTP, FTP, SMTP, DNS PresentationSSL/TLS, ASCII, Unicode SessionAuthentication, Authorization, Session setup TransportTCP, UDP NetworkIP, ICMP, OSPF, BGP Data LinkEthernet, IEEE 802.3, MAC addresses PhysicalNetwork cables, hubs, NICs