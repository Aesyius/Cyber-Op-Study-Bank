The DHCP server prevents IP address conflicts by keeping a record of addresses it has assigned and ensuring that each client receives a unique address from the pool. If a conflict occurs, it can be resolved by releasing or renewing the address.